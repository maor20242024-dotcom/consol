module.exports = {
  i18n: {
    defaultLocale: 'en', // Default language: English
    locales: ['en', 'ar'], // Supported languages
    localeDetection: true, // Enable automatic locale detection based on browser
  },
  localeExtension: 'json', // Translation file extension
};