# =============================================================================
# IMPERIUM GATE - Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Node Environment
# -----------------------------------------------------------------------------
NODE_ENV="development"

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → API
# Documentation: https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL (safe for client-side)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"

# Supabase anonymous/public key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# Supabase service role key (KEEP SECRET - server-side only!)
# ⚠️ CRITICAL: Never expose this key in client-side code
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED for Prisma)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → Database → Connection String

# Pooler Connection (for runtime queries via Prisma)
# Format: postgresql://postgres.xxxxx:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres:[password]@db.your-project-id.supabase.co:6543/postgres?pgbouncer=true"

# Direct Connection (for migrations and db push)
# Format: postgresql://postgres.xxxxx:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DIRECT_URL="postgresql://postgres:[password]@db.your-project-id.supabase.co:5432/postgres"

# -----------------------------------------------------------------------------
# NextAuth Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Only needed if using NextAuth for authentication
# NEXTAUTH_URL should match your production domain

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secure-random-string-here"

# -----------------------------------------------------------------------------
# AI Services Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Only needed if using AI features (voice assistant, content generation)

# OpenRouter API Key - for AI routing services
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY="your-openrouter-api-key-here"

# Google Gemini API Key - for Google AI services
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# -----------------------------------------------------------------------------
# Instagram Ads Integration (OPTIONAL)
# -----------------------------------------------------------------------------
# Only needed if integrating with Instagram Ads API
# Get from: https://developers.facebook.com/

INSTAGRAM_CLIENT_ID="your-instagram-client-id"
INSTAGRAM_CLIENT_SECRET="your-instagram-client-secret"
INSTAGRAM_ACCESS_TOKEN="your-instagram-access-token"

# -----------------------------------------------------------------------------
# Additional Configuration
# -----------------------------------------------------------------------------

# Application URL (for production deployment)
# Update this when deploying to production
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Logging level (development, production, debug)
LOG_LEVEL="info"

# -----------------------------------------------------------------------------
# DEPLOYMENT NOTES
# -----------------------------------------------------------------------------
# 
# For Vercel Deployment:
# 1. Add all NEXT_PUBLIC_* variables in Vercel dashboard
# 2. Add DATABASE_URL and DIRECT_URL
# 3. Add SUPABASE_SERVICE_ROLE_KEY (encrypted)
# 4. Update NEXT_PUBLIC_APP_URL to your Vercel domain
# 5. Ensure NODE_ENV is set to "production"
#
# For Docker Deployment:
# 1. Create .env file with all variables
# 2. Mount as volume or use --env-file flag
# 3. Never include .env in the Docker image
#
# Security Checklist:
# ✓ Never commit .env.local or .env files
# ✓ Rotate keys if accidentally exposed
# ✓ Use encrypted environment variables in CI/CD
# ✓ Restrict service role key to backend only
# ✓ Use different keys for staging and production
# =============================================================================
